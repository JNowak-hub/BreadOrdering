import styled from "styled-components";

const NavbarWrapper = styled.nav`
  height: 50px;
  background: pink;
  color: black;
  display: flex;
  > * {
    flex: 1;
  }
`;

export default NavbarWrapper;